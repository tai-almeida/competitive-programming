#include <iostream>
using namespace std;

int main() {
    int counter = 0, letters;
    string new_char;
    cin>>letters;
    for(int i=0; i<letters; i++) {
        cin>>new_char;
        if(new_char )
    }
}